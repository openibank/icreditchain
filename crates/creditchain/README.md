# CreditChain Command Line Interface (CLI) Tool

The `creditchain` tool is a command line interface (CLI) for debugging, development, and node operation.

See [CreditChain CLI Documentation](https://github.com/creditchainorg/creditchain/tree/main/docs) for how to install the `creditchain` CLI tool and how to use it.
